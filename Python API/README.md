# Creating an RESTful API using Python
In this project I built a small web-based system that allows users to be added to and retrieved from a database through a web address (API). This was constructed using Flask and SQLAlchemy.
